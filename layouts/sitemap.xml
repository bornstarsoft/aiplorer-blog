{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- $seen := newScratch }}
  {{- range .Data.Pages }}
    {{- $permalink := .Permalink }}
    {{- if and $permalink (not ($seen.Get $permalink)) }}
      {{- $seen.Set $permalink true }}
  <url>
    <loc>{{ $permalink }}</loc>
      {{- with .Lastmod }}
        {{- if not .IsZero }}
    <lastmod>{{ .Format "2006-01-02T15:04:05-07:00" }}</lastmod>
        {{- end }}
      {{- end }}
  </url>
    {{- end }}
  {{- end }}
</urlset>
